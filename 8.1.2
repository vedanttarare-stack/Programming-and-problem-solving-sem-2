start=int(input())
stop=int(input())
found= False
for num in range(start, stop+1):
	if num > 1:
		for i in range(2, int(num**0.5)+1):
			if num % i == 0:
				break
		else:
			print(num)
			found = True
if not found:
	print("No primes\n")
